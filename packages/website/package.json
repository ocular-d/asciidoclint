{
    "name": "@testthedocs/docs",
    "version": "0.1.0",
    "description": "Documentation for the AsciiDoc linter using Antora",
    "license": "MIT",
    "private": true,
    "scripts": {
        "build": "antora antora-playbook.yml",
        "dev": "antora antora-playbook.yml --to-dir=./build/site --clean",
        "serve": "http-server build/site -c-1 -p 9090",
        "watch": "nodemon --watch src --watch antora-playbook.yml --ext adoc,yml,yaml,js --exec \"pnpm run dev\"",
        "clean": "rm -rf build"
    },
    "devDependencies": {
        "@antora/cli": "^3.1.14",
        "@antora/site-generator": "^3.1.14",
        "http-server": "^14.1.1",
        "nodemon": "^3.1.14"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "@antora/lunr-extension": "1.0.0-alpha.13",
        "@asciidoctor/tabs": "1.0.0-beta.6"
    }
}